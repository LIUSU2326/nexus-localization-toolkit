# TransMate 开发守则

本守则定义 TransMate 后续版本的产品目标、翻译与检测边界、复杂度预算和发布门槛。任何涉及文本翻译、译后检测、自动修复、问题分类、任务流程或交付门禁的修改，都必须遵守本文件。

## 1. 产品目标

TransMate 面向普通本地化操作人员，而不是规则工程师。默认工作流必须保持：

```text
上传文件 → 点击翻译 → 内部确定性 QA → 自动完成并保存
                                         ↓
                          可选：点击检测语言质量建议
```

- 用户不需要理解问题分类、语言学规则、模型参数或修复轮次。
- 正常任务不得要求用户反复导入报告、勾选问题类型或确认每轮修复。
- 高级设置可以存在，但必须折叠且不影响默认流程成功运行。
- 内部实现可以复杂，用户交互不能因此复杂。
- “可选检测”只指语义、流畅度和建议复核；确定性 QA 与交付门禁始终在翻译流程内部执行。
- 唯一允许的两个自动终态：
  - 高置信硬问题为零：自动保存正式交付版和报告。
  - 修复预算耗尽或检测冲突：自动保存 `_unverified` 当前最佳译文和报告，明确标注不是正式交付版。

## 2. 一步到位原则

- 首译后由工具自动执行必要的本地确定性检查。
- 确认存在高置信硬问题时，只处理对应单元格，不得重跑已通过文本。
- 同一单元格的多个问题必须合并为一次逻辑修复尝试；多个单元格可以共享一个微批请求。
- 候选译文只有在问题严格减少且没有新增硬问题时才能替换当前最佳译文。
- 已通过或已接受的译文立即冻结，后续检测不得再次调度。
- 所有入口必须共用一份按单元格记录的全局修复账本，不能让首译补救、微批回退、深修和连续清零分别拥有独立预算。
- 首译后的 AI 内容候选默认最多生成一次。批返回缺 ID、空值或结构错误且尚未产生内容候选时，允许用一次单条请求替代该次尝试；候选已返回但被质量门禁拒绝后，不得再生成新候选。
- 极少数确定性硬问题如需第二个内容候选，必须由统一策略显式授权、证明第一候选已净减少硬问题且没有回归，并将全局上限固定为二。不得由调用入口自行决定。
- 网络重试只在没有内容候选时发生，拥有独立但固定的全任务上限；不得计入或绕过内容候选预算。
- 达到修复预算仍未解决时，保留当前最佳译文并结束任务；不得以重复重译掩盖检测器冲突。
- 任务应自动完成剩余内容并输出结果及审计信息，不把逐条判断压力转移给用户。

## 3. 检测规则分层

检测结果只能分为以下三层：

### 3.1 高置信硬问题

只有能够被稳定、重复、语言环境化验证的问题才能阻断交付或触发自动修复，例如：

- 空译文、模型未返回或明确失败标记。
- 占位符、格式标签明确缺失或多出。编号/命名占位符默认只校验身份和数量；只有不可换序结构 token、标签嵌套或项目明确声明的顺序约束才能把顺序变化判为硬问题。
- 经过目标语言数字格式归一后的数字、正负号、比较符、百分比或区间语义错误。
- 目标语言与残留文字系统明确不同，且已排除品牌、代码、项目术语和合法共用字符。

硬问题规则必须优先保证精确率。无法证明的情况不得进入本层。

### 3.2 项目约束

以下内容由项目术语和保留策略决定，不得使用全局规则强制重译：

- 人名、品牌、音译名、玩法名和世界观专名。
- LV、HP、DEF、BOSS、CD 等 UI 标记或游戏缩写。
- 指定术语、专有大小写和项目级保留英文。

项目约束只有在项目规则显式声明 `enforcement=hard`、作用域精确命中并给出允许目标变体时才能阻断。参考列推断、未声明缩写和猜测得到的专名只能建议复核。

### 3.3 建议复核

启发式或语言学不确定项只能作为建议，默认不阻断、不自动调用 AI：

- 单个英文词、借词、同形词或疑似错误语种。
- 疑似内容不完整、译文过短、译文超长。
- 流畅度、语气、自然程度和风格偏好。
- 同文字系统语言之间仅凭词形作出的判断。
- 未知或无法稳定归类的问题。

“不知道”必须表现为建议复核，不能表现为必须重译。

## 4. 多语言规则原则

- 禁止通过不断添加 `if (targetLang === ...)` 和单词白名单修补问题。
- 禁止因为某个报告出现 `limit`、`list`、`Arena` 等词，就直接加入全局例外。
- 通用检测逻辑与语言数据必须分离。
- 语言差异必须收敛到唯一的 `language-quality-profiles` 配置层。最小 Schema 为：`profileVersion`、`scripts`、`tokenizer`、`numberSeparators`、`units`、`placeholderPolicy`、`standardLexiconVersion`。
- 语言配置缺失或无法稳定判断时必须降为建议复核，禁止回退到“与英文参考词重合即报错”。
- 人名、项目词和单份报告词条不得进入语言配置；它们只能进入有作用域和版本号的项目策略。
- `profileVersion` 与项目策略版本必须进入缓存键、续接签名和报告审计信息。
- 受影响语言族必须有正反例，并至少选择一个无关语言作为哨兵；只有跨文字系统的通用规则才要求运行全语言族矩阵。
- 同文字系统语言不得仅凭单词与英语参考相同就判定为错误语种。
- 人名和专名默认进入项目约束或建议复核，除非项目明确要求本地化或音译。

## 5. AI 使用边界

- AI 负责生成候选译文，不负责定义确定性事实。
- 检测器的误判不得通过修改翻译提示词来掩盖。
- 不得调度与确定问题无关的全文重译。修复接口可以返回完整最终句，但必须以当前译文为基线，并通过语义、结构和硬问题不退化门禁；不得依赖不可验证的“请最小修改”提示词保证安全。
- 修复提示必须包含原文、当前最佳译文和本次确定问题，要求最小必要修改。
- 批量修复必须使用稳定 ID，并逐项恢复占位符、逐项质检、逐项决定是否采用。
- 请求失败、结构错误与内容质量失败必须分别计数，不得互相放大重试。
- 额度耗尽、取消或致命错误必须立即停止新增请求，并安全保留已完成结果。

## 6. 停止、回滚与冻结

- 每个逻辑单元格只有一个“当前最佳译文”。
- 输出槽和稳定任务身份只由文件、工作表、目标语言、原文列与单元格位置决定，不得包含 API 通道、模型名称或重试执行者。
- 多选 API 通道只能用于分担唯一任务队列、限流或故障接管；不得把同一逻辑单元格复制成多份默认译文，也不得因更换执行通道新增输出列。
- 新候选未改善、出现振荡或新增硬问题时，必须回滚。
- 同一问题重复出现且没有净改善时，内部标记为检测冲突并停止；不得继续生成新候选。
- 复检本身不得自动创建新的全量翻译任务。
- 用户确认保留的项目项应生成项目范围内的决定记录，避免重复出现，但不得污染其他项目或语言。
- 冻结只在同一文件内容签名、目标语言、语言配置版本、项目规则版本和术语版本下有效；上下文明确变化时必须重新验证。

## 7. 时间与请求效率

质量与数据安全是硬约束。在不违反硬约束的前提下，按端到端瓶颈优化用户操作、耗时、API 请求和页面性能：

1. 用户操作次数最少。
2. 总任务耗时最短。
3. API 请求和费用最少。
4. 页面渲染和报告生成性能。

- 不能用增加重复翻译次数换取表面上的“问题清零”。
- 优先减少需要调用 AI 的文本数量，其次才是提高并发。
- 精确同文复用、已通过冻结、微批请求和只补失败项是允许的性能优化。
- 任何性能改动都必须同时统计候选采用率和新增问题率；只报告“处理项/分钟”不构成成功。

## 8. 复杂度预算

- 翻译质量架构必须只有一个 canonical issue registry、一个 delivery classifier、一个 candidate gate 和一个 per-cell attempt ledger。UI、报告导入、重试队列和交付模块不得复制问题正则或各自重新分类。
- 新方案必须替换或删除旧路径，不得在旧流程外继续叠加另一套长期流程。
- 安全迁移可以短期保留适配层，但必须写明负责人、删除版本和禁止成为第二默认路径；没有删除计划不得合入。
- 新增一个设置、状态或问题类别，必须证明它减少了用户操作或解决了可复现缺陷。
- 单个语言、单个词或单份报告的特殊补丁默认拒绝。
- 能通过数据配置解决的问题，不得增加新的控制流分支。
- 能通过删除低精度规则解决的问题，不得增加补偿规则。
- 默认流程不得新增启动确认弹窗；仅语言冲突、覆盖、删除和改变交付资格等风险动作允许确认。

### 8.1 v1.5.3 旧输出迁移层

- 负责人：TransMate 桌面翻译工作流。
- 适用范围：只读兼容 `version < 5` 的 profile-bound 进度键，以及带旧式“目标语言 · 通道 · 模型”生成列的内嵌报告工作簿。
- 迁移结果：先按逻辑单元格选择一个当前最佳译文，再写入唯一的 `primary` 输出槽；被淘汰候选只进入“历史候选迁移审计”，不得成为第二份默认译文。
- 禁止事项：迁移层不得创建 profile 专属任务、输出列或重试预算，不得进入新文件的常规写入路径。
- 删除计划：profile-bound 任务键适配器由桌面工作流在 v1.7.0 删除；届时仅保留旧文件格式的只读列清理器，且该清理器不得参与任务身份、候选选择或输出生成。
- v1.5.5 起任务键使用工作表内部原始行坐标；仅在恢复 v1.5.4 本地进度时只读接受一次基于合并全局行号的 `legacyTaskKey`。该适配同样在 v1.7.0 删除，不得写入新报告、进入候选身份或参与交付计数。

### 8.2 v1.5.7 修复生命周期迁移层

- 负责人：TransMate 桌面翻译工作流。
- `v1.5.7` 报告必须持久化每个阻断单元格的上下文签名、finding 指纹、候选快照、候选判定和终止决定；相同输入和规则上下文不得再次调用 AI。
- 不含生命周期的旧报告只允许进入一次旧报告迁移尝试；本轮生成的新报告必须写入生命周期，不能继续产生无预算的旧格式状态。
- 检测器拒绝了已返回候选时，状态必须是“质量阻断/检测冲突”，不得伪装成“接口未返回”；预算耗尽后输出 `_unverified` 当前最佳结果和报告。
- 删除计划：旧报告的无生命周期续接适配器在 `v1.7.0` 删除；旧文件仍可只读查看，但不能自动创建新的修复预算。

## 9. 修改前的证据要求

新增或升级检测规则前必须提供：

- 至少一个真实问题样本。
- 至少一个相似但正确、不得误判的反例。
- 涉及语言的代表性正确译文、借词、专名和真实残留样本。
- 对现有报告的离线统计，说明预计减少的漏判和可能增加的误判。
- 为什么不能通过删除、降级或项目配置解决。

没有反例测试的检测规则不得进入硬问题层。

## 10. 必须观察的指标

指标必须由版本化质量回放统一计算，不能由功能代码自行解释：

- 首译通过率：首次生成后高置信硬问题为零的唯一源单元格数 / 首译唯一源单元格数。
- 高置信硬问题精确率与召回率：按人工金标 finding 计算，不按报告行数计算。
- 自动修复候选采用率：通过 canonical candidate gate 的内容候选数 / 已生成内容候选数；该指标只能用于诊断，不能单独追求更高。
- 修复后新增硬问题率：已接受候选中新出现硬 finding 的单元格数 / 已接受候选单元格数。
- 内容改写次数：按逻辑单元格统计 AI 返回的内容候选数，网络和结构重试不得混入。
- 每千条文本 API 请求数：实际请求数 / 唯一源单元格数 × 1000。
- 单语言端到端耗时：固定设备和通道配置下，从点击翻译到终态产物保存的 p50 与 p95。
- 用户交互数：默认流程中必须执行的点击、确认和人工判断数量。

问题总数下降不能单独作为成功指标。

发布最低门槛：

- 负例金标中新增硬误判必须为零。
- 确定性正例必须全部检出。
- 已接受候选新增硬问题必须为零。
- 首译后每个单元格内容候选次数的 p100 必须符合全局 attempt ledger 上限。
- 耗时或请求数相对版本化基线回归超过 10% 时必须在发布说明中明确批准，不能静默发布。

## 11. 回归测试与发布门槛

- 每条硬规则必须同时有正例和反例测试。
- 必须覆盖拉丁文字、斯拉夫文字、阿拉伯文字、CJK、印度文字和东南亚文字的代表语言。
- 必须覆盖人名、品牌、借词、缩写、占位符、数字、区间、折扣和真实漏译。
- 必须使用历史真实报告做离线回放，确认已接受译文不会重新进入自动修复。
- 默认用户流程必须保持“上传 → 翻译 → 可选检测”。
- 不得存在无上限重试、整表循环重译或复检自动触发全量任务。
- `npm test`、Web 构建、桌面构建和无控制台错误检查必须通过。
- 若硬规则在回归集中出现误判，必须修正或降级后才能发布。

质量基础设施必须提供：

- `fixtures/quality/manifest.json`：匿名化、版本化的正例、反例、语言族和人工金标清单。
- `npm run test:quality-replay`：输出机器可读 metrics JSON，并与版本化 baseline 比较。
- 默认流 E2E：上传 fixture 后一次点击到达终态，断言普通启动确认数为零、问题类别选择数为零、产物类型与交付门禁一致。
- 发布候选必须运行 `npm run desktop:build:windows`（Windows）或对应平台桌面构建命令。

在上述质量回放命令和资产建立前，新增或升级硬检测规则不得发布。

## 12. 后续版本的调整顺序

1. 建立 `fixtures/quality`、人工金标、质量回放命令和当前版本基线，不改翻译提示词。
2. 建立 canonical issue registry、delivery classifier、candidate gate 与全局 attempt ledger，并让所有入口统一消费。
3. 删除或降级低精度硬规则，收缩自动修复范围。
4. 建立 `language-quality-profiles` Schema，将语言差异迁移为声明式配置，移除逐词和逐语言控制流补丁。
5. 将内部流程收敛为“首译、确定性检查、一次逻辑定向修复、回滚或冻结”。
6. 建立默认流 E2E 后，再依据真实瓶颈优化批量大小、请求并发和界面性能。
